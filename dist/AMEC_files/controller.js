/**
 * And of course we define a controller for our route.
 */
angular.module('ngBoilerplate.controllers', [])
	.controller( 'VideoCtrl', function VideoController($scope, videochanges) {
		//Just an example of talking between 2 directives but never used
		$scope.uivideoControl = {};

	});
